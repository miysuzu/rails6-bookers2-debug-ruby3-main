<div class="container px-5 px-sm-0">
  <div class="row">
    <!-- 左カラム -->
    <div class="col-md-3">
      <h4>User info</h4>
      <%= render 'info', user: current_user %>

      <h4 class="mt-4">New book</h4>
      <%= render 'books/form', book: @book %>
    </div>

    <!-- 右カラム -->
    <div class="col-md-8 offset-md-1">
      <h2>Users</h2>
      <%= render 'index', users: @users %>
    </div>
  </div>
</div>
